import styled from 'styled-components';
import { closedSidebarWidth } from 'styles/theme';

const SidebarOffset = styled.div`
  margin-left: ${closedSidebarWidth}px;
`;

export default SidebarOffset;
