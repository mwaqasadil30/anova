import { ReactComponent as ExpandIcon } from 'assets/icons/white-double-arrow.svg';
import { ReactComponent as EditIcon } from 'assets/icons/white-pencil-edit.svg';
import styled from 'styled-components';

// Removed hard-coded colours
export const StyledEditIcon = styled(EditIcon)``;
// Removed hard-coded colours
export const StyledExpandIcon = styled(ExpandIcon)``;
