export interface Values {
  emailAddress: string;
  emailDomain: string;
  firstName: string;
  lastName: string;
  company: string;
  applicationTimeoutHours: number | '';
  applicationTimeoutMinutes: number | '';
}
