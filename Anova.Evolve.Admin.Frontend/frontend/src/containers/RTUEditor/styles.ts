import Typography from '@material-ui/core/Typography';
import styled from 'styled-components';

export const StyledActionColumnText = styled(Typography)`
  font-size: 20px;
  font-weight: 400;
`;

export const StyledUnderlinedCellText = styled(Typography)`
  font-size: 14px;
  text-decoration: underline;
`;
