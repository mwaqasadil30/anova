import type { InitOptions } from 'i18next';

// IMPORTANT NOTE: Any changes made here need to be synced up to
// the `i18next-scanner.config.js` file
const i18nDefaults: InitOptions = {
  keySeparator: false,
};

export default i18nDefaults;
